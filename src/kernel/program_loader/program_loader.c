#include "program_loader.h"

#include "kernel/file_system/file_system.h"

void load_program(const char* path)
{
    /*FILE* file = kfopen(path);	
	if (efiFile == NULL)
	{
		Print(L"ERROR: Failed to load %s\n\r", path);
				
		while (1)
		{
			__asm__("HLT");
		}
	}*/
}